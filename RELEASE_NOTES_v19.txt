D2Synergy Sprint 1 v19

Implemented:
- Separate desktop and mobile presentation modules loaded by a shared layout controller.
- Desktop/mobile presentation CSS split so only the active layout stylesheet applies.
- Rebuilt horizontal character-card treatment and D2Synergy-styled Finalize Loadout action.
- Rebuilt right-side drawer behavior and eliminated the unreadable white mobile menu styling.
- Added a permanent global toast implementation so account/live-sync code no longer throws when Vault code has not loaded.
- Desktop Builder uses a dense multi-column analytical workspace.
- Mobile Builder uses a dedicated compact presentation with fixed bottom workspace navigation.
- Desktop Vault uses a wider grid-first presentation.
- Mobile Vault uses a dedicated compact grid and mobile item-detail sheet behavior.
- Updated application, asset, and service-worker versions to v19.

Notes:
- Shared Bungie API, state, inventory, transfers, loadouts, and synergy calculations remain unchanged.
- This is the first architecture/visual sprint, not completion of all later product phases.
