D2Synergy v19.0.1 Emergency Rollback

This package restores the last stable v18.2.1 application code and removes the broken Sprint 1 presentation split. It uses a new cache/version identifier so browsers replace the broken v19 files.

Restored:
- Bungie sign-in and OAuth flow
- Existing live profile data
- Builder and Vault functionality
- Stable navigation and overlays

The desktop/mobile split will be rebuilt incrementally after this rollback is confirmed stable.
